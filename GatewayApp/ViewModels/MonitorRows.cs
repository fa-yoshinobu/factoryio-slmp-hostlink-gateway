using GatewayApp.Models;

namespace GatewayApp.ViewModels;

public sealed record MonitorBoolRow(MappingEntry? Left, MappingEntry? Right);

public sealed record MonitorRegisterRow(MappingEntry? Left, MappingEntry? Right);

