<Window x:Class="GatewayApp.Views.Dialogs.CsvImportPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:GatewayApp.Converters"
        Title="CSV インポート プレビュー"
        Width="780"
        Height="520"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <conv:DisplayTypeTextConverter x:Key="DisplayTypeTextConverter" />
    </Window.Resources>
    <DockPanel Background="#ebebeb">
        <TextBlock DockPanel.Dock="Top"
                   x:Name="SummaryText"
                   Margin="14,12,14,8"
                   Foreground="#222"
                   FontWeight="Bold" />
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="14,10,14,14">
            <Button Content="キャンセル" Width="96" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="適用" Width="96" IsDefault="True" Click="Apply_Click" />
        </StackPanel>
        <DataGrid x:Name="PreviewGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Margin="14,0"
                  LoadingRow="PreviewGrid_LoadingRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="処理" Binding="{Binding ActionText}" Width="90" />
                <DataGridTextColumn Header="Modbus" Binding="{Binding ModbusLabel}" Width="120" />
                <DataGridTextColumn Header="名前" Binding="{Binding Name}" Width="2*" />
                <DataGridTemplateColumn Header="表示型" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayType, Converter={StaticResource DisplayTypeTextConverter}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="既存 PLC" Binding="{Binding ExistingPlcAddress}" Width="120" />
                <DataGridTextColumn Header="理由" Binding="{Binding Reason}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
